version: '3'

services:
  cube:
    image: cubejs/cube:v0.35
    ports:
      - "4000:4000"
      - "3000:3000"
    environment:
      - CUBEJS_DB_TYPE=postgres
      - CUBEJS_DB_HOST=aws-0-us-east-1.pooler.supabase.com
      - CUBEJS_DB_PORT=5432
      - CUBEJS_DB_NAME=postgres
      - CUBEJS_DB_USER=postgres.mtmmzylgtyipvcdkpimf
      - CUBEJS_DB_PASS=Tibs2026!!!
      - CUBEJS_DEV_MODE=true
      - CUBEJS_API_SECRET=crmtibs_secret_key_2026_xyz
    volumes:
      - ./cube/schema:/cube/conf/model
